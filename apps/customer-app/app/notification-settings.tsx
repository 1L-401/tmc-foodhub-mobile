import React from 'react';

import { SettingsPlaceholderScreen } from '@/components/settings/settings-placeholder-screen';

export default function NotificationSettingsScreen() {
  return (
    <SettingsPlaceholderScreen
      title="Notifications"
      description="Notification categories and delivery preferences will be configurable here."
    />
  );
}